import styled from "styled-components";

export const Input = styled.input`
  width: 75%;
  background: #84aeff;
  border: none;
  padding-left: 20px;
  &:focus{
    outline: 0;
    outline-offset: 0;
  }
`