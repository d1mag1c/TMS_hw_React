import React from 'react';
import {BlockTitle, StyleTitle} from "./style";

const Title = () => {
    return (
        <BlockTitle>
            <StyleTitle>Sign in</StyleTitle>
        </BlockTitle>
    );
};

export default Title;