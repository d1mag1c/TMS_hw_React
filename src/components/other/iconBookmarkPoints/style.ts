import styled from "styled-components";

export const BookmarkPointsBlock = styled.div`
  display: flex;
  align-items: center;
  justify-content: end;
  height: 50px;
  svg{
    margin: 0 10px;
    &:hover {
      fill: red;
      cursor: pointer;
    }
  }
    `

export const IconWrapper = styled.div`
    
    `